apply plugin: 'java'
apply plugin: 'maven'

group='com.github.blueapron'

sourceCompatibility = rootProject.ext.sourceCompatibilityVersion
targetCompatibility = rootProject.ext.sourceCompatibilityVersion

tasks.withType(JavaCompile) {
    options.warnings = false
}

